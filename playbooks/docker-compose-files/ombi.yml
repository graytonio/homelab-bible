version: "3.8"
services:
  ombi:
    image: lscr.io/linuxserver/ombi:latest
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/New_York
    volumes:
      - ombi_config:/config
    ports:
      - "3579:3579"
    restart: unless-stopped
volumes:
  ombi_config: